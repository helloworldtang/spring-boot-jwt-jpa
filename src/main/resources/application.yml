
jwt:
  route:
    authentication:
      path: auth
      refresh: refresh

logging:
  file: logs/selectdata.log
  level:
      root: info
server:
  port: 80
spring:
  profiles:
      active: @activatedProperties@,swagger,actuator


